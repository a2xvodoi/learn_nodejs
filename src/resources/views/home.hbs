<div class="container">
    <div class="row mt-5">
        {{#each Cources}}
        <div class="col-md-6 col-lg-4">
            <div class="card" >
            <a href="/cources/{{this.slug}}"><img src="{{this.image}}" class="card-img-top" height="200rem" alt="{{this.name}}"></a>
            <div class="card-body">
                <a href="/cources/{{this.slug}}"><h5 class="card-title">{{this.name}}</h5></a>
                <p class="card-text">{{this.decripsions}}</p>
                <a href="#" class="btn btn-primary">Mua khóa học</a>
            </div>
        </div>
        </div>
        
        {{/each}}

    </div>
    </div>